.side-bar
	.header
		%h3.header__name
			= current_user.name
		%li.list
		= link_to do
			= fa_icon 'pencil-square-o', class: 'icon'
		%li.list
		= link_to edit_user.path(current_user) do
			= fa_icon 'cog', class: 'icon'
	.groups
		.group
			.group__name
			グループ名
			.group__message
			message
		.group
			.group__name
			グループ名
			.group__message
			メッセージ
		.group
			.group__name
			グループ名
			.group__message
			message
			
